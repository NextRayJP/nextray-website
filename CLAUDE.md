# nextray-website — NextRay公式サイト（nextray.jp）

**このサイトが事業方針の一次情報。** 料金・サービス内容・受付上限を変えるときは、ここを最初に直してから、Notion・メモリ・SNSプロフィールへ反映する。逆順にしない。

- リポジトリ: `NextRayJP/nextray-website`（main）／公開: GitHub Pages ＋ `CNAME`（nextray.jp）
- 静的HTMLのみ。ビルド工程なし。**`git push` した瞬間に本番公開される**

## CSSの構造（ここを間違えやすい）

| ファイル | 役割 |
|---|---|
| `assets/site.css`（547行） | **全ページ共通。** ヘッダー・フッター・料金表・ブログの体裁 |
| `assets/home.css`（339行） | **トップページ専用。** `index.html` だけが追加で読む |
| `sample-*.html` | **CSSを外部参照しない。** 各ファイル内の `<style>` で完結する見込み客向けデモ。site.cssの影響を受けない |

`hp-*.html`（業種別ランディング）は `site.css` のみ。**共通CSSを触ると42枚のHTMLすべてに影響する**ので、1ページだけの調整なら、そのページ側で閉じる方法を先に検討する。

## レイアウト規約

- **セクションの背景は白→薄グレーの交互リズム**を守る。連続して同じ背景を置かない
- **改行制御は `.sp` クラス**（`site.css:18` で `display:none`、狭い画面でのみ表示）。`<br>` を直接置かない
- **ブレークポイントは 1000px**。ナビゲーションと料金グリッド（`.ptier-grid`）がここで切り替わる。他の値を新たに増やさない
- 料金表のクラス名は `ptier-` 接頭辞。似た名前を増やすと衝突するので、既存クラスを再利用する

## 書き方の決まりごと

- **一人称は「私」**。「私たち」は使わない（永久に個人で運営するため。コミット `3e5ce9f` で統一済み）
- **数字は盛らない。** Anthropic Academy は「20コース修了」（過去に「認定23件」と書いて修正した経緯あり）
- 掲載する金額：ホームページ制作 **初期費用0円＋月額29,800円（税別・最低12ヶ月）**／AI自動化 **月額198,000円（税別）**
- 更新依頼への対応は **「2営業日以内」**（2026-07-28に「7日以内」から短縮）
- 受付上限は **HP制作50案件＋AI自動化50案件＝合計100案件**。単位は「社」ではなく**「案件」**

## ファイルの種類

- `blog-*.html` — コラム記事。`blog.html` が一覧
- `hp-*.html` — 業種別ランディング（カフェ・歯科・工務店・飲食・サロン・税理士）
- `sample-*.html` — 同じ6業種のデモサイト。**商談用の主力オファー**。見込み客向けデモを作るときはこれを土台にする
- `service-*.html` / `soumu-*.html` — サービス説明
- `_*.md` — 公開されないメモ（SNSプロフィール原稿・記事下書き・設定手順）
- 日本語名のフォルダ（`こだわり`・`プライバシーポリシー`・`会社案内`・`cookie-クッキー-ポリシー`）— **旧Wixサイトの日本語URLからの転送ページ**。消すと外部リンクが404になる。触らない

## 変更したら

1. `sitemap.xml` にページを追加したか確認（新規ページの場合）
2. Notionハブの「🔧 Claudeの変更履歴」に1行書く
3. push は指示を待つ
